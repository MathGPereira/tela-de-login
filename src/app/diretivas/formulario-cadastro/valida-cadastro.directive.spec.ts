import { ValidaCadastroDirective } from './valida-cadastro.directive';

describe('ValidaCadastroDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidaCadastroDirective();
    expect(directive).toBeTruthy();
  });
});
