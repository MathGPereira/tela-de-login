import { ValidaLoginDirective } from './valida-login.directive';

describe('ValidaLoginDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidaLoginDirective();
    expect(directive).toBeTruthy();
  });
});
